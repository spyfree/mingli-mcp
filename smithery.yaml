# Smithery configuration for Mingli MCP Server
# For Python runtime, only 'runtime' and 'env' fields are allowed

# Python runtime - no configuration required!
runtime: python

# Optional environment variables (no configuration required to run)
env:
  LOG_LEVEL: INFO
  TRANSPORT_TYPE: stdio
