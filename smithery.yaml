# Smithery configuration for Mingli MCP Server
# Minimal configuration that works with Smithery

runtime: python

env:
  TRANSPORT_TYPE: "http"
  HTTP_HOST: "0.0.0.0"
  HTTP_PORT: "8080"
  LOG_LEVEL: "INFO"
